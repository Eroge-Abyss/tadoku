on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          # This tells it to handle both Rust and Node
          manifest-config: .release-please-manifest.json
          config-file: release-please-config.json
          # Use a PAT so that the tag push triggers the release.yml workflow.
          # GITHUB_TOKEN does not trigger downstream workflows by design.
          token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          # Create releases as drafts so artifacts can be uploaded before going public
          draft: true
    permissions:
      pull-requests: write
      contents: write
